{% extends 'base.html.twig' %}

{% block body %}
    <h1>Competence creation</h1>
<div class="col-lg-8 box-form">
    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}
 </div>
    <ul>
        <li>
            <a href="{{ path('competence_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
